# 页面手工测试执行记录

## 1. 执行环境

| 项目 | 内容 |
| --- | --- |
| 执行日期 | 2026-07-07 |
| 前端框架 | React + Vite + Ant Design |
| 后端框架 | Flask |
| 浏览器 | 本地浏览器 |
| 前端地址 | `http://127.0.0.1:5173` |
| 后端地址 | `http://127.0.0.1:5000` |

## 2. 手工测试记录

| 执行编号 | 关联用例 | 操作步骤 | 期望结果 | 实际结果 | 状态 |
| --- | --- | --- | --- | --- | --- |
| MANUAL-001 | TC-REG-001 | 进入 `/register`，输入 `testuser`、`Password123`、`Password123`，点击注册 | 注册成功并跳转登录页 | 注册成功并跳转登录页 | 通过 |
| MANUAL-002 | TC-REG-002 | 进入 `/register`，用户名留空，输入密码并点击注册 | 页面提示 `用户名不能为空` | 页面提示 `用户名不能为空` | 通过 |
| MANUAL-003 | TC-REG-003 | 进入 `/register`，输入 `abc`、`Password123`、`Password123`，点击注册 | 注册失败，提示 `用户名长度不能少于6位` | 注册成功并跳转登录页 | 失败 |
| MANUAL-004 | TC-REG-005 | 进入 `/register`，输入两次不同密码并点击注册 | 页面提示 `两次输入的密码不一致` | 页面提示 `两次输入的密码不一致` | 通过 |
| MANUAL-005 | TC-LOGIN-001 | 使用已注册用户 `testuser` 和正确密码登录 | 登录成功并进入首页 | 登录成功并进入首页 | 通过 |
| MANUAL-006 | TC-LOGIN-002 | 使用已注册用户 `testuser` 和错误密码登录 | 登录失败，提示 `用户名或密码错误` | 登录失败，提示 `用户名或密码错误` | 通过 |
| MANUAL-007 | TC-LOGOUT-001 | 登录后在首页点击退出登录 | 返回登录页面 | 返回登录页面 | 通过 |

## 3. 页面缺陷记录

| 项目 | 内容 |
| --- | --- |
| 缺陷编号 | BUG-001 |
| 触发步骤 | 注册页面输入短用户名 `abc` 并提交 |
| 期望结果 | 注册失败，提示 `用户名长度不能少于6位` |
| 实际结果 | 注册成功 |
| 影响页面 | `/register` |

